<%= form_for(@usersbook) do |f| %>
  <% if @usersbook.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@usersbook.errors.count, "error") %> prohibited this usersbook from being saved:</h2>

      <ul>
      <% @usersbook.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :usuário %><br />
    <%= f.collection_select(:users_id, User.all, :id, :login) %>
	
  </div>
  <div class="field">
    <%= f.label :título %><br />
    <%= f.collection_select(:books_id, Book.all, :id, :title) %>

  </div>
  <div class="field">
    <%= f.label :valor %><br />
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
